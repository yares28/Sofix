# Copy to .env.docker (git-ignored) for `docker compose --env-file .env.docker up`.
# Local containers only: never put Neon URLs here.
POSTGRES_PASSWORD=change-me-to-a-long-random-string
FOOTBALL_DATA_ORG_TOKEN=
FOOTBALL_DATA_ORG_COMPETITION=PD
